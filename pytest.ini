[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (use real data backtest)
    integration: marks tests as integration (requires network, real Binance data)

