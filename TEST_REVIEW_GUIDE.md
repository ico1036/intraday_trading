# 테스트 코드 리뷰 가이드 (Top-Down)

## 📋 리뷰 목표

**"이 테스트가 실제로 버그를 잡을 수 있는가?"**
- 유저 입장에서 "이럴땐 이래야 맞다!" 원칙 검증
- 실제 시나리오 반영 여부
- Mock 사용 최소화 (실제 데이터 기반)
- 통과를 위한 테스트가 아닌 진짜 검증

---

## 🏗️ 리뷰 순서 (Top-Down)

### Level 1: 전체 아키텍처 테스트 (통합)
**목적**: 전체 시스템이 올바르게 동작하는지 확인

#### 1.1 `test_runner.py` - ForwardRunner 통합 테스트
**검토 포인트**:
- [ ] 실제 Orderbook → MarketState 변환 검증
- [ ] 전략 실행 → 주문 생성 → PaperTrader 제출 플로우 검증
- [ ] 체결 데이터 → LIMIT 주문 체결 플로우 검증
- [ ] 완전한 거래 사이클 성과 리포트 검증
- [ ] Mock 사용 여부 (있으면 제거 필요)

**체크리스트**:
```python
# ✅ 좋은 예: 실제 시나리오
def test_get_performance_report_with_trades(self):
    # 1. 매수 신호 → LIMIT BUY 주문 생성
    # 2. 가격 하락 → 체결
    # 3. 매도 신호 → LIMIT SELL 주문 생성
    # 4. 가격 상승 → 체결
    # 5. 성과 리포트 확인
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_get_performance_report_with_trades(self):
    # 단순히 리포트가 생성되는지만 확인
    assert report.total_trades >= 0  # 의미 없는 검증
```

---

### Level 2: 핵심 비즈니스 로직 테스트
**목적**: 각 컴포넌트가 올바른 로직을 수행하는지 확인

#### 2.1 `test_strategy.py` - 전략 로직 테스트
**검토 포인트**:
- [ ] OBI 전략: imbalance 임계값 정확히 검증
- [ ] 경계값 테스트 (exact threshold)
- [ ] 실제 MarketState 데이터 사용 여부
- [ ] Mock/MagicMock 사용 여부

**체크리스트**:
```python
# ✅ 좋은 예: 실제 시나리오
def test_generate_buy_order_when_imbalance_high(self):
    # imbalance = 0.5 > 0.3 (buy_threshold)
    # → BUY 주문 생성되어야 함
    # → LIMIT 주문, best_bid에 지정가
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_generate_buy_order_when_imbalance_high(self):
    # 단순히 주문이 생성되는지만 확인
    assert order is not None  # 의미 없는 검증
```

#### 2.2 `test_strategy_example_real.py` - 전략 예시 실제 시나리오
**검토 포인트**:
- [ ] Stop Loss: 가격 하락 시 손절 실행 검증
- [ ] Take Profit: 가격 상승 시 익절 실행 검증
- [ ] 포지션 있을 때 신규 진입 차단 검증
- [ ] 완전한 거래 사이클 성과 측정
- [ ] Trailing Stop: 최고가 추적 검증

**체크리스트**:
```python
# ✅ 좋은 예: 유저 입장 검증
def test_buy_entry_then_stop_loss_triggers(self):
    """
    유저 입장: "100000에 매수했는데 2% 하락(98000)하면 손절해야지!"
    """
    # 1. 매수 진입 @ 100000
    # 2. 가격 하락 @ 98000 (2% 손실)
    # 3. 손절 주문 생성 확인
    # 4. 손절 주문 체결 확인
    # 5. 손실 확인 (음수 PnL)
```

---

### Level 3: 데이터 구조 및 계산 테스트
**목적**: 데이터 모델과 계산 로직 정확성 확인

#### 3.1 `test_paper_trader.py` - 거래 시뮬레이터 테스트
**검토 포인트**:
- [ ] MARKET 주문: 즉시 체결 검증
- [ ] LIMIT 주문: 조건 충족 시 체결 검증
- [ ] PnL 계산: 수수료 포함 정확한 계산
- [ ] 포지션 관리: 진입/청산/추가 진입 검증
- [ ] 실제 시장 가격 사용 여부

**체크리스트**:
```python
# ✅ 좋은 예: 실제 시나리오
def test_limit_buy_filled_when_price_drops(self):
    # LIMIT BUY @ 99000
    # 가격이 98900으로 떨어짐
    # → 체결되어야 함
    # → 포지션 생성 확인
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_limit_buy_filled_when_price_drops(self):
    # 단순히 체결 여부만 확인
    assert trade is not None  # 의미 없는 검증
```

#### 3.2 `test_paper_trader_queue.py` - 주문 큐 테스트
**검토 포인트**:
- [ ] FIFO 순서 검증
- [ ] 여러 주문 동시 체결 검증
- [ ] 주문 취소 기능 검증
- [ ] TTL 만료 검증
- [ ] Market Making 시나리오 검증

**체크리스트**:
```python
# ✅ 좋은 예: 실제 시나리오
def test_first_order_filled_first(self):
    # order1 @ 99000, order2 @ 98000
    # 가격이 98500으로 떨어짐
    # → order1만 체결 (FIFO)
    # → order2는 대기 중
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_first_order_filled_first(self):
    # 단순히 주문이 큐에 있는지만 확인
    assert len(trader.pending_orders) == 2  # 의미 없는 검증
```

#### 3.3 `test_performance.py` - 성과 계산 테스트
**검토 포인트**:
- [ ] 수익률 계산 정확성
- [ ] 승률 계산 정확성
- [ ] Profit Factor 계산 정확성
- [ ] Max Drawdown 계산 정확성
- [ ] 수수료 포함 계산 검증

**체크리스트**:
```python
# ✅ 좋은 예: 실제 계산 검증
def test_total_return_calculation(self):
    # 초기 자본: 10000
    # 거래 후: 10098
    # 수익률: (10098 - 10000) / 10000 * 100 = 0.98%
    assert report.total_return == pytest.approx(0.98, rel=0.01)
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_total_return_calculation(self):
    # 단순히 리포트가 생성되는지만 확인
    assert report.total_return >= 0  # 의미 없는 검증
```

---

### Level 4: 데이터 소스 및 파싱 테스트
**목적**: 외부 데이터 수신 및 파싱 정확성 확인

#### 4.1 `test_client_basic.py` - WebSocket 클라이언트 테스트
**검토 포인트**:
- [ ] Orderbook 파싱 정확성
- [ ] AggTrade 파싱 정확성
- [ ] 에러 케이스 처리 (빈 데이터, 누락 필드)
- [ ] 실제 Binance 데이터 형식 반영 여부

**체크리스트**:
```python
# ✅ 좋은 예: 실제 데이터 형식 검증
def test_parse_orderbook_normal(self):
    # 실제 Binance WebSocket 응답 형식
    # bids/asks 배열, timestamp, last_update_id 등
    # 모든 필드 정확히 파싱되는지 확인
    
# ❌ 나쁜 예: 통과를 위한 테스트
def test_parse_orderbook_normal(self):
    # 단순히 파싱이 성공하는지만 확인
    assert snapshot is not None  # 의미 없는 검증
```

---

## 🔍 리뷰 체크리스트 (각 테스트마다)

### 1. 테스트 목적 명확성
- [ ] 테스트 이름이 무엇을 검증하는지 명확한가?
- [ ] Docstring에 "유저 입장" 시나리오가 명시되어 있는가?

### 2. 실제 시나리오 반영
- [ ] 실제 시장 상황을 반영하는가?
- [ ] Mock/MagicMock 사용 최소화 (실제 데이터 사용)
- [ ] 경계값 테스트 포함 (exact threshold, edge cases)

### 3. 검증의 완전성
- [ ] 단순히 "None이 아니다"가 아닌 구체적인 값 검증
- [ ] 수익/손실, 수수료 등 실제 계산 검증
- [ ] 상태 변화 검증 (포지션, 주문 큐 등)

### 4. 버그 발견 가능성
- [ ] 이 테스트가 실제 버그를 잡을 수 있는가?
- [ ] 로직 변경 시 테스트가 실패하는가?
- [ ] 통과를 위한 테스트가 아닌가?

---

## 📝 리뷰 예시

### ✅ 좋은 테스트 예시

```python
def test_buy_entry_then_stop_loss_triggers(self):
    """
    시나리오: 매수 진입 → 가격 하락 → 손절 실행
    
    유저 입장: "100000에 매수했는데 2% 하락(98000)하면 손절해야지!"
    """
    strategy = OBIStrategyWithStopLoss(stop_loss_pct=0.02)
    trader = PaperTrader(initial_capital=10000.0)
    
    # 1. 매수 진입
    state1 = MarketState(mid_price=100000.0, imbalance=0.5, ...)
    order1 = strategy.generate_order(state1)
    trader.submit_order(order1)
    trade1 = trader.on_price_update(...)
    
    # 2. 가격 하락 → 손절 주문 생성
    strategy._position_quantity = trader.position.quantity
    state2 = MarketState(mid_price=98000.0, ...)  # 2% 하락
    order2 = strategy.generate_order(state2)
    
    assert order2 is not None
    assert order2.side == Side.SELL  # 손절 청산
    assert order2.order_type == OrderType.MARKET
    
    # 3. 손절 주문 체결
    trade2 = trader.on_price_update(...)
    assert trade2.pnl < 0  # 손실 확인
```

**왜 좋은가?**
- ✅ 실제 시나리오 반영 (매수 → 하락 → 손절)
- ✅ 유저 입장 명시 ("100000에 매수했는데...")
- ✅ 구체적인 값 검증 (SIDE, ORDER_TYPE, PnL)
- ✅ 버그 발견 가능 (손절 로직 변경 시 실패)

### ❌ 나쁜 테스트 예시

```python
def test_stop_loss(self):
    """Stop loss 테스트"""
    strategy = OBIStrategyWithStopLoss()
    order = strategy.generate_order(MarketState(...))
    assert order is not None  # 의미 없는 검증
```

**왜 나쁜가?**
- ❌ 테스트 목적 불명확
- ❌ 실제 시나리오 반영 없음
- ❌ 구체적인 검증 없음 (단순히 None 체크)
- ❌ 버그 발견 불가능 (로직 변경해도 통과)

---

## 🎯 리뷰 시작 방법

### Step 1: 전체 구조 파악
```bash
# 테스트 파일 목록 확인
ls tests/test_*.py

# 각 파일의 테스트 개수 확인
uv run pytest tests/ --collect-only
```

### Step 2: Level 1부터 순차적으로 리뷰
1. `test_runner.py` - 통합 테스트부터 시작
2. 각 테스트를 읽으며 위 체크리스트 확인
3. 문제 발견 시 즉시 수정 또는 이슈 기록

### Step 3: 리뷰 결과 정리
- 각 테스트 파일별 리뷰 결과 기록
- 개선 사항 TODO 리스트 작성
- 우선순위 정하기 (Critical → Medium → Low)

---

## 💡 리뷰 팁

1. **"이 테스트가 없으면 어떤 버그를 놓칠까?"** 질문하기
2. **실제 시나리오로 바꿔보기**: "이 테스트가 실제로 이런 상황을 검증하는가?"
3. **Mock 제거하기**: Mock이 있으면 실제 데이터로 바꿀 수 있는지 확인
4. **경계값 추가하기**: exact threshold, edge cases 테스트 추가
5. **성과 검증 강화**: 단순히 "동작함"이 아닌 "올바른 결과" 검증

---

## 📊 리뷰 진행 상황 추적

각 파일별로 체크:

- [ ] `test_runner.py` - 통합 테스트
- [ ] `test_strategy.py` - 전략 로직
- [ ] `test_strategy_example_real.py` - 전략 예시 실제 시나리오
- [ ] `test_paper_trader.py` - 거래 시뮬레이터
- [ ] `test_paper_trader_queue.py` - 주문 큐
- [ ] `test_performance.py` - 성과 계산
- [ ] `test_client_basic.py` - WebSocket 클라이언트

---

**리뷰 원칙**: "이 테스트가 실제 버그를 잡을 수 있는가?"

